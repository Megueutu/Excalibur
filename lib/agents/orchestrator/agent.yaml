name: orchestrator
description: Use at the start of any implementation task in an Excalibur project, to classify the task, decide which pipeline layers actually run, and build the handoff for each agent that does. Not for executing the work itself — it delegates and never edits project code.
tools: Read, Grep, Glob, Task
personas:
  - guardrail
  - systems-thinker
model: inherit
